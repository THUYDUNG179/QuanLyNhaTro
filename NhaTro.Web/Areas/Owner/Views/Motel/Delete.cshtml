```cshtml
@model NhaTro.Application.DTOs.MotelDto
@{
    ViewData["Title"] = "Xóa nhà trọ";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Xóa nhà trọ</h2>
<h4>Bạn có chắc muốn xóa nhà trọ này?</h4>
<div>
    <dl class="row">
        <dt class="col-sm-2">Tên nhà trọ</dt>
        <dd class="col-sm-10">@Model.MotelName</dd>
        <dt class="col-sm-2">Địa chỉ</dt>
        <dd class="col-sm-10">@Model.Address</dd>
    </dl>
    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="MotelId" />
        <button type="submit" class="btn btn-danger">Xóa</button>
        <a asp-action="Index" class="btn btn-secondary">Hủy</a>
    </form>
</div>
```